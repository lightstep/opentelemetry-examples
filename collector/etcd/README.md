ETCD and OTEL collector: a ETCD cluster and single etcd docker-compose are provided they can be ran as
`docker compose -f docker-compose.cluster.yaml up` or `docker compose -f docker-compose.single.yaml up`
respectively.
